/* Regular Challenge 3 */

// Create an array that has at least 5 strings:
// Use a for loop to console log all of the elements plus the index and a comma

let housewives=['Orange County', 'New York', 'Atlanta', 'New Jersey', 'Beverly Hills'];

for (let i = 0; i <= housewives.length; i++) {
  console.log(i + "," + housewives[i]);
}
